apiVersion: v1
kind: Service
metadata:
  name: wikijs-service
  labels:
    app: wikijs
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    app: wikijs
